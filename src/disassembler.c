#include "disassembler.h"

void escribirDisassembler(t_MV maquina, short tamano)
{
    int tamOpA, tamOpB, pc = 0, codMnemonic;
    while (pc < tamano)
    {
        codMnemonic = maquina.memoria[pc];
    }
}